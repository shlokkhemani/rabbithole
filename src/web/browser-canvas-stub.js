// PDF.js reaches this module only from a Node-only branch. Keeping the browser
// target explicit prevents a platform-dependent native `canvas` installation
// from changing the emitted chunk graph.
